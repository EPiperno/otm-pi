[Z]
description = Vertical Z linear stage (M1/M2)
degrees_per_step = 1.8
microsteps_per_full_step = 8
recommended_style = DOUBLE
screw_pitch = 2.54   # Z: mm per revolution
max_travel_mm = 150.0            # TODO: set real usable travel
default_speed_mm_s = 5
ramp_fraction = 0
current_limit_amps = 1.3
voltage = 5.0

[THETA]
description = Rotational theta stage (M3/M4)
degrees_per_step = 1.8
microsteps_per_full_step = 8
recommended_style = DOUBLE
screw_pitch = 1   # THETA: deg per revolution
default_speed_deg_s = 5
ramp_fraction = 0
current_limit_amps = 1.3
voltage = 5.0

[camera]
backend = daheng
index = 1        # 1-based; change if you have multiple cameras
resolution = 500X500
fps = 30
serial =               # Optional Daheng camera serial overrides index if set
jpeg_quality = 40       # 40-95; lower = faster/smaller
downscale = 1           # Integer factor to shrink frames (2 halves width/height)
frame_skip = 0          # Skip N frames between sends (0 = none)
roi =                   # Optional ROI x,y,w,h (e.g. 200,150,800,600)
exposure_us =           # Initial exposure in microseconds (Daheng); if using OpenCV backend convert logic applies
gain_db =               # Initial gain in dB

